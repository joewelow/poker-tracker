from setuptools import setup, find_packages

setup(
    name="poker-tracker",
    version="0.1",
    description="A package to track poker results",
    author="Joewe Low",
    # author_email="",
    # url="",
    packages=find_packages(),
    install_requires=[
    ],
)
